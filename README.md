# roman-numeral-calculator
A modular Python Roman Numeral Calculator that converts Roman numerals to integers, performs arithmetic operations (+, -, *, /, ^), and returns results in Roman numeral format. Includes input validation, range enforcement (1–3999), quotient and remainder handling, and structured error management.
